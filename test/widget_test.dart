import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MetroWake app smoke test', (WidgetTester tester) async {
    // Note: This test requires MetroDataProvider to be initialized
    // which needs Provider setup. See route_service_test.dart for
    // comprehensive tests that don't require widget setup.
  });
}
